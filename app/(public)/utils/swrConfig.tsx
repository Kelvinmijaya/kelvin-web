const SwrConfig = {
  initialSize: 1,
  revalidateAll: true,
  revalidateFirstPage: false,
  revalidateIfStale: true,
  revalidateOnReconnect: true,
  persistSize: false,
  parallel: false,
}

export default SwrConfig
